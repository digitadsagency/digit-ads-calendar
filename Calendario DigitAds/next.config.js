/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router está habilitado por defecto en Next.js 14
  // No necesitamos la configuración experimental
}

module.exports = nextConfig
